---
name: ocv
description: >
  OpenClaw Version Control - Backup and restore your AI agent with natural language.
  Tell your agent "save" or "restore" in Feishu/Telegram/WhatsApp, and it handles the rest.
argument-hint: "[save|restore|status|log|rollback|auto|init] [args]"
---

# OCV — OpenClaw Version Control

## 一句话说明

在飞书/Telegram/WhatsApp 里说"保存一下"，AI Agent 就帮你把整个 ~/.openclaw/ 备份到 git。

---

## 触发条件 (Triggers)

当用户说起以下任何内容时，触发 OCV：

| 用户说 | 意图 | 执行命令 |
|--------|------|----------|
| "保存 / 备份 / snapshot" | 保存当前状态 | `ocv save` |
| "保存一下，备注xxx" | 带消息保存 | `ocv save "xxx"` |
| "恢复 / 回滚" | 恢复版本 | `ocv restore` 或 `ocv rollback` |
| "看看改了什么 / status" | 查看变更状态 | `ocv status` |
| "最近的保存记录 / 历史" | 查看历史 | `ocv log` |
| "打开/关闭自动保存" | 自动保存开关 | `ocv auto on/off` |
| "/ocv xxx" | 斜杠命令直通 | 直接执行对应命令 |

---

## 命令映射表

```
用户意图                          →  exec 调用
─────────────────────────────────────────────────────
"保存一下"                       →  ocv save
"保存一下，备注展会版"            →  ocv save "展会版"
"看看改了什么"                   →  ocv status
"最近的保存记录"                 →  ocv log
"回滚到 abc1234"                →  ocv rollback abc1234 --yes
"回滚到上一个版本"               →  ocv rollback --yes
"打开自动保存"                   →  ocv auto on
"关闭自动保存"                   →  ocv auto off
"自动保存状态"                   →  ocv auto status
"在新机器恢复"                   →  ocv restore <url>
─────────────────────────────────────────────────────
```

---

## Agent 回复指南

### ocv save 执行后

```
💾 已保存! a3f8b21 — 5 个文件有变更 → 已推送
```

如果没有变更：
```
💾 自上次保存以来没有变化
```

### ocv status 执行后

```
上次保存: 2小时前
3 个文件有变更:
  📝 workspace/AGENTS.md
  📝 workspace/skills/seo-agent/SKILL.md
  🆕 workspace/memory/2026-02-28.md
```

### ocv log 执行后

```
最近 5 次保存:
a3f8b21  2小时前   💾 优化了SEO策略
b7c2e45  5小时前   💾 auto-save
c1d3f67  昨天      💾 展会demo调整
d5e7a89  昨天      💾 加入多机位切换
e9f1b23  前天      💾 auto-save
```

### ocv rollback 执行后

```
⏪ 已回滚到 c1d3f67 (展会demo调整)
撤销方法: 告诉我 "回滚到 a3f8b21"
```

### ocv restore 执行后

```
🦞 OCV Restore
═══════════════════════════
✅ Cloned from: git@github.com:user/my-openclaw.git
✅ Files restored: 127
✅ Credentials permissions fixed

🎉 恢复完成! 运行 openclaw 启动。
```

---

## 输出规范

- 所有回复保持简短，不超过 5 行
- 使用 emoji 增强可读性
- hash 使用短格式 (7位)
- 错误信息简洁，不暴露敏感路径

---

## 安全提醒

Agent 在首次引导用户时必须提醒：

> ⚠️ 备份会包含所有文件（包括 API Keys）。请确保使用**私有 git 仓库**！
